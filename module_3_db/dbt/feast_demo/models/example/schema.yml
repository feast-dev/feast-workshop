
version: 2

models:
  - name: aggregate_transaction_features
    description: ""
    columns:
      - name: "user_id"
        description: "The primary key for this table"
        tests:
          - not_null
